{
  "name": "node-ec6-seed",
  "version": "1.0.0",
  "description": "Nodejs 6 and Ecmascipt 6 seed",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [
    "node",
    "ecmascript6"
  ],
  "author": "Esteban Garcia Warnecke",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "mongoose": "^4.11.4",
    "rimraf": "^2.6.1"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.0"
  }
}
